@import '~app/assets/stylus'


.root
  propertyPadding('padding')

  h3
    font-size: 24px
    margin-bottom: 8px

  .payment
    display: inline-block

    &:not(:first-of-type)
      margin-left: 8px

    &:not(:last-of-type)
      margin-right: 8px
